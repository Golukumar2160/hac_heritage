# BHARAT-DRISHTI Production Requirements (Python 3.11+)
# ─────────────────────────────────────────────────────
# IMPORTANT: sentence-transformers (PyTorch), xgboost, and mlflow are intentionally
# EXCLUDED from this file. They are pipeline-only tools used in pipelines/fraud_models.py
# which runs as an offline subprocess, NEVER inside the Uvicorn web server process.
# Install those via: pip install -r requirements-pipeline.txt (local / Render background worker only)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
PyJWT>=2.9.0
cryptography>=43.0.0

# Data & Forensics Analysis
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
scipy>=1.13.0
joblib>=1.4.0
networkx>=3.2
plotly>=5.20.0

# Database & Cloud Connectivity
psycopg2-binary>=2.9.9
supabase>=2.8.0

# Document & Computer Vision
pymupdf>=1.24.0
rapidocr-onnxruntime>=1.3.0
ImageHash>=4.3.1
pillow>=10.4.0
qrcode[pil]>=7.4.2
reportlab>=4.2.0

# AI & LLM Integration
google-genai>=0.1.1
requests>=2.32.0
